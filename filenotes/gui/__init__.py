"""PySide6 desktop GUI for filenotes (optional; install with the ``gui`` extra).

Nothing here is imported by the CLI, so the command-line tools stay free of any
Qt dependency. The entry point is :func:`filenotes.gui.app.main` (``notes-gui``).
"""
